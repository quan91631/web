document.addEventListener('DOMContentLoaded', () => {
    // Initialize pagination with the total number of pages you need
    const pagination = new Pagination(20); // Adjust the number based on your needs
}); 